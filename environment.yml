name: rocm-pytorch
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - --index-url https://download.pytorch.org/whl/rocm6.4
      - torch
      - torchvision
      - torchaudio